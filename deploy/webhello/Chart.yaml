apiVersion: v2
name: webhello
description: Reference example tool -- plain static HTML/JS frontend (no egui/wasm), deployed via the shared tool-library chart.
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: tool-library
    version: 0.1.0
    repository: "file://../charts/tool-library"
