apiVersion: v2
name: idp
description: >-
  The tools platform's own OIDC provider + WebAuthn passkey login, deployed
  via the shared tool-library chart like any other tool.
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: tool-library
    version: 0.1.0
    repository: "file://../charts/tool-library"
